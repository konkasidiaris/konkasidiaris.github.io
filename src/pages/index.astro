---
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { getAllPosts, sortMDByDate } from "@/utils";

const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

// const kkTech: Array<{ title: string; desc: string; href: string }> = [
// 	{
// 		title: "Mongo",
// 		desc: "Build fast websites, faster.",
// 		href: "https://astro.build",
// 	},
// ];
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">Hello World!</h1>
		<p class="mb-4">
			My name is Konstantinos Kasidiaris. I know that greek names are both sesquipedalian (😅) and difficult to pronounce. You can call me <strong>Kon</strong> (pronounced as constant) from now on!
			I see that you found my little internet corner! I write about stuff that either exite me or given me a hard time in the past.
			I like web technologies a lot. My biggest dream is to find my own <strong>42</strong>. Until then, I will continue to contribute to applications that improve people's lives.
		</p>
		<SocialList />
	</section>
	<section aria-label="Blog post list" class="mt-16">
		<h2 class="title mb-4 text-xl">Posts</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>
	<!-- <section class="mt-16">
		<h2 class="title mb-4 text-xl">Tools in my current toolset</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				kkTech.map(({ href, title, desc }) => (
					<li>
						<a
							href={href}
							target="_blank"
							rel="noopener noreferrer"
							class="kk-link inline-block"
						>
							{title}
						</a>: 
						<p class="inline-block sm:mt-2">{desc}</p>
					</li>
				))
			}
		</ul>
	</section> -->
</PageLayout>
